{
  "name": "classhub",
  "version": "1.0.0",
  "description": "api for classhub",
  "main": "index.js",
  "scripts": {
    "create": "claudia create --region eu-central-1 --api-module api --version dev --policies policies --configure-db",
    "deploy": "claudia update --version dev",
    "reconfigure": "claudia update --configure-db --version dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.4.11",
    "claudia-api-builder": "^2.0.2",
    "shortid": "^2.2.6"
  },
  "devDependencies": {
    "jasmine": "^2.5.2",
    "mock-require": "^1.3.0"
  }
}
